<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.samplebootapp.infrastructure.datasource.order.OrderMapper">

    <insert id="insertOrder">
        INSERT INTO order_orders (id, user_id, status, total_amount, ordered_at)
        VALUES (#{id}, #{userId}, #{status}, #{totalAmount}, #{orderedAt})
    </insert>

    <insert id="insertOrderItems">
        INSERT INTO order_order_items (order_id, product_id, product_name, price, quantity)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{orderId}, #{item.productId.value}, #{item.productName}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>

</mapper>
