<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.samplebootapp.infrastructure.cart.mapper.CartMapper">

    <resultMap id="CartResultMap" type="com.example.samplebootapp.domain.cart.model.Cart">
        <id property="id" column="id" typeHandler="com.example.samplebootapp.infrastructure.cart.mapper.typehandler.CartIdTypeHandler"/>
        <result property="userId" column="user_id"/>
        <collection property="items" ofType="com.example.samplebootapp.domain.cart.model.CartItem" select="findItemsByCartId" column="id"/>
    </resultMap>

    <resultMap id="CartItemResultMap" type="com.example.samplebootapp.domain.cart.model.CartItem">
        <constructor>
            <arg column="product_id" javaType="String"/>
            <arg column="quantity" javaType="int"/>
        </constructor>
    </resultMap>

    <insert id="insertCart">
        INSERT INTO order_carts (id, user_id)
        VALUES (#{id.value}, #{userId})
    </insert>

    <delete id="deleteCartItems">
        DELETE FROM order_cart_items WHERE cart_id = #{cartId}
    </delete>

    <insert id="insertCartItem">
        INSERT INTO order_cart_items (cart_id, product_id, quantity)
        VALUES (#{cartId}, #{item.productId}, #{item.quantity})
    </insert>

    <select id="findByUserId" resultMap="CartResultMap">
        SELECT id, user_id
        FROM order_carts
        WHERE user_id = #{userId}
    </select>

    <select id="findItemsByCartId" resultMap="CartItemResultMap">
        SELECT product_id, quantity
        FROM order_cart_items
        WHERE cart_id = #{cartId}
    </select>

</mapper>
