name: PR Check

on:
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: write

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Run Unit Tests
        run: ./gradlew test

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            build/test-results/test
            build/reports/tests/test
            build/reports/jacoco/test/html

      - name: Upload JaCoCo Execution Data
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-exec
          path: build/jacoco/test.exec

  checkstyle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Run Checkstyle
        run: ./gradlew checkstyleMain

      - name: Upload Checkstyle Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-report
          path: build/reports/checkstyle

  pmd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Run PMD
        run: ./gradlew pmdMain

      - name: Upload PMD Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pmd-report
          path: build/reports/pmd

  spotbugs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Run SpotBugs
        run: ./gradlew spotbugsMain

      - name: Upload SpotBugs Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: spotbugs-report
          path: build/reports/spotbugs

  # coverage:
  #   runs-on: ubuntu-latest
  #   needs: unit-test
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Set up JDK 17
  #       uses: actions/setup-java@v4
  #       with:
  #         java-version: "17"
  #         distribution: "temurin"

  #     - name: Setup Gradle
  #       uses: gradle/actions/setup-gradle@v3
  #       with:
  #         cache-read-only: false

  #     - name: Download JaCoCo Execution Data
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: jacoco-exec
  #         path: build/jacoco

  #     - name: Run Coverage Report
  #       # -x test is strictly required to avoid re-running tests.
  #       # -x integrationTest is required because jacocoTestReport depends on it in build.gradle
  #       run: ./gradlew jacocoTestReport -x test -x integrationTest

  #     - name: Upload Coverage Report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: coverage-report-all
  #         path: build/reports/jacoco

  deploy-docs:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: [unit-test, checkstyle, pmd, spotbugs, coverage]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages (Placeholder)
        run: echo "Docs deployment logic here if needed, currently just verifying other jobs"
